import React from 'react';
import { render, fireEvent, screen } from '@testing-library/react';
import QAListView from '../QAListView';

// describe('QAList View Test', () => {
//   render(<QAListView />);
//   const questions = rendered.container.getElementsByClassName('qLine');

//   test('Should only display at most 4 questions on initial render', () => {
//     expect(questions.length < 4).toBe(true);
//   });

// });
